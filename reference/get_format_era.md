# Get format era for a given year

Returns the data format era for processing. All data comes from GOSA.

## Usage

``` r
get_format_era(end_year)
```

## Arguments

- end_year:

  School year end

## Value

Character string indicating era (always "gosa" for supported years)
